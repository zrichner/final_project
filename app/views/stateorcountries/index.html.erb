<div class="page-header">
  <h1>
    Stateorcountries
    <a href="/stateorcountries/new" class="btn btn-lg btn-success">New Stateorcountry</a>
  </h1>
</div><div class="row mb-1">
  <div class="col-md-12">
    <%= search_form_for @q, :class => collapse_search_form, :id => "stateorcountries_filters" do |f| %>
      <p class="lead">Narrow results:</p>

        <div class="form-group">
    <%= f.label :name_cont, "Name contains" %>
    <%= f.text_field :name_cont, :class => "form-control", :placeholder => "Name contains" %>
  </div>

<div class="form-group">
          <%= f.label :created_at, "Created at" %>
          <div class="row">
            <div class='col-md-6 mb-1'>
              <div class='input-group date' id='created_atgteqPicker'>
                  <%= f.text_field :created_at_gteq, :class => "form-control", :placeholder => "Created at greater than or equal to" %>
                  <span class="input-group-addon">
                      <span class="glyphicon glyphicon-calendar"></span>
                  </span>
              </div>
            </div>

            <div class='col-md-6'>
              <div class='input-group date' id='created_atlteqPicker'>
                  <%= f.text_field :created_at_lteq, :class => "form-control", :placeholder => "Created at less than or equal to" %>
                  <span class="input-group-addon">
                      <span class="glyphicon glyphicon-calendar"></span>
                  </span>
              </div>
            </div>
          </div>
      </div>
      <script type="text/javascript">
          $(function () {
              $('#created_atgteqPicker').datetimepicker({
                  format: 'YYYY-MM-DD hh:mm:SS'
              });
              $('#created_atlteqPicker').datetimepicker({
                  useCurrent: false,
                  format: 'YYYY-MM-DD hh:mm:SS'
              });
              $('#created_atgteqPicker').on("dp.change", function (e) {
                  $('#created_atlteqPicker').data("DateTimePicker").minDate(e.date);
              });
              $('#created_atlteqPicker').on("dp.change", function (e) {
                  $('#created_atgteqPicker').data("DateTimePicker").maxDate(e.date);
              });
          });
      </script>

        <div class="form-group">
      <%= f.label :cities_name_cont, "City name contains" %>
      <%= f.text_field :cities_name_cont, :class => "form-control", :placeholder => "City name contains" %>
    </div>

<div class="form-group">
      <%= f.label :users_email_cont, "User email contains" %>
      <%= f.text_field :users_email_cont, :class => "form-control", :placeholder => "User email contains" %>
    </div>

<div class="form-group">
      <%= f.label :region_name_cont, "Region name contains" %>
      <%= f.text_field :region_name_cont, :class => "form-control", :placeholder => "Region name contains" %>
    </div>

      <%= f.submit :class => "btn btn-primary btn-block mb-1" %>

      <a href="/stateorcountries" class="btn btn-default btn-block mb-1">Clear filters</a>
    <% end %>
    <a class="btn btn-default btn-block mb-1" data-toggle="collapse" href="#stateorcountries_filters">
      Show Filters
    </a>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Name</th>
        <th>Region</th>
        <th>Actions</th>
      </tr>

      <% @stateorcountries.each do |stateorcountry| %>
      <tr>
        <td><%= stateorcountry.name %></td>
        <td>
          <% if stateorcountry.region.present? %>
            <a href="/regions/<%= stateorcountry.region.id %>">
              <%= stateorcountry.region.name %>
            </a>
          <% end %>
        </td>
        <td>
          <a href="/stateorcountries/<%= stateorcountry.id %>" class="btn btn-primary">Show</a>
          <a href="/stateorcountries/<%= stateorcountry.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_stateorcountry/<%= stateorcountry.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
    <%= paginate @stateorcountries, theme: 'twitter-bootstrap-4' %>
  </div>
</div>
